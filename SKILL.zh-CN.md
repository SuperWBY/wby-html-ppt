# WBY HTML PPT

中文 | [English](SKILL.md)

沉淀制作方法，而非套用这次案例的版式。固定的是交互合同、验收与交付能力；风格、构图、素材和动画由内容决定。用户明确指定的视觉与交互要求优先。

## 工作方式

先判断用户要方案还是执行：“给方案”先给文案与画面意图；“做出来、修改、添加”直接落实到已有项目。已提供的材料与决定不重复询问。仅当缺失信息影响事实或重要方向时澄清，同时推进不依赖答案的工作。

1. 读取现有项目、页面顺序、相关材料与已确认风格。续做必须定位源文件，不从打包产物逆向重做整套。
2. 明确观众、讲解目标、当前页在叙事中的作用和下一页衔接。区分事实、用户判断、教学示意及待核实内容。
3. 按 `references/visual-method.md` 判断表达方式。每页内部形成简短制作判断：展示对象、观众应理解的关系、证据/素材、构图、实现手段。无需把内部判断全部展示给用户。
4. 逐页制作和预览；只调整授权范围。整套任务持续推进，不把逐页执行变成逐页索要许可。保存已确认页面，只为跨页一致性做必要联动修改。
5. 依据用户反馈修改内容、顺序、节奏和表现形式，而非只换颜色。新增、删除和重排同步更新唯一页面清单。
6. 根据 `references/interaction-delivery.md` 验收实际页面、交互与最终单文件；交付可维护源项目和可转发 HTML。明确哪些检查确实完成、哪些被环境阻止。

## 能力协同

先查当前会话可用的技能目录，使用真实路径读取，不能把名字视为自动执行。

- **oil-ppt**：存在时用于单页结构、页面增删顺序、CLI 状态检查。遵循其当前接口，不复制它的实现。它的参考配色和 starter 是参考，不把某套课件的视觉强加给所有课件。独立播放器通过本 Skill 的模板生成，不覆盖其生成的预览文件。
- **oil-tone**：用于面向观众的文案。保持用户的观点与讲者身份，不套入其他作者的名字或经历。
- **imagegen**：需要新人物、场景或工具插画时读取并调用内置生图工具；结合本套视觉方向，素材存入项目。不要用程序占位图冒充用户要求的生图。
- **HTML/CSS/SVG**：准确文字、流程、对照、时间线、气泡和可修改图形优先代码绘制。
- **浏览器工具**：实际检查排版、键盘、全屏、总览、图片放大。使用当前允许的浏览器能力。

缺少可选技能时说明影响，按本 Skill 完成可行部分；不能声称使用了不可用能力，不自动安装。以 HTML 为默认成品；用户要求 PPTX 时另用相应导出流程并说明可编辑性限制。

## 固定演示能力

任何新交付默认包含：左右键翻页、全屏按钮/F、O 总览和缩略图跳转、Esc 关闭浮层、截图放大、外部链接入口、页码与边界状态、独立 HTML 打包。编辑框中不截获输入；焦点进入幻灯片后快捷键仍有效。不得为满足入口数量编造文档或应用链接。

提供动画能力但不强制每页动：翻页与浮层可轻量过渡；解释顺序、变化、重点时选择性添加内容动画。禁止自动翻页和干扰阅读的循环效果，除非用户明确要求；尊重 `prefers-reduced-motion`。细则见 `references/interaction-delivery.md`。

## 可复用实现

`assets/player.html` 是交互外壳，不是页面设计模板。使用标准 HTML 独立页面，样式以单页作用域隔离。宽高/风格由项目决定，播放器默认 16:9 可修改。

准备 UTF-8 JSON 清单：

```json
{"title":"演示标题","slides":["slides/intro.html","slides/example.html"]}
```

路径相对项目目录，数组只列实际交付页面。不要把历史草稿或未确认页面自动加入。已使用 oil-ppt 时可直接用其 deck.json，但先核实交付范围。

```sh
python3 <本Skill目录>/scripts/build.py <项目目录> --manifest <清单路径> --output <输出HTML路径>
```

脚本内嵌本地 CSS、普通脚本、图片和 CSS 图片，资产去重；保持外部超链接。明确不支持的资源会报错，不静默产出缺资源文件。包裹层可由模板定制交互视觉，但保留固定功能。

## 完成判断

- 没有将参考案例转化为固定人物、颜色、卡片数量或构图。
- 插画服务讲解；真实证据保真；示意明确标注，不编造历史截图。
- 相邻页关系清楚，文字/图像投影可读，无裁切、溢出或无意义重复。
- 最终单文件功能经过测试；外部文档仍需网络和权限，不宣称打包可以绕过权限。
